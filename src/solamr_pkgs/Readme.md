# Required Pkgs for solamr_simualtor

- aruco_detect
```
sudo apt install ros-<distro>-aruco-detect
```

- Joy
```
sudo apt install ros-<distro>-aruco-joy
``` 

- effort_controller
```
sudo apt install ros-<distro>-effort-controllers
```
